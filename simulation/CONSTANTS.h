#pragma once

#include <cmath>

// Stores constants for entire program

// Speed of light in meters per second
const float SPEED_OF_LIGHT = 299'792'458;
// Light year in meters
const double LIGHT_YEAR = 9.461 * std::pow(10, 15);